# Required for ARM64: Path to Chromium executable
# Puppeteer doesn't provide Chrome binaries for ARM64, so use system Chromium
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium

# Optional: Enable performance monitoring and debug logging
# DEBUG=1
